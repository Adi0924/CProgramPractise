#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("The area of the circle is %f\n", pi * radius * radius);
    int heigth = 5;
    printf("Volume of the Cylinder is %f\n", pi * radius * radius * heigth);
    return 0;
}